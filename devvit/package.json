{
  "name": "tipsy-devvit",
  "version": "0.0.1",
  "private": true,
  "description": "TIPSY — a clumsy delivery, on Reddit via Devvit",
  "scripts": {
    "build": "tsc --noEmit && devvit build",
    "dev": "devvit playtest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@devvit/public-api": "^0.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
