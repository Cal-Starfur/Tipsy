name: tipsy-delivery
version: 0.0.1
homepage: https://reddit.com
description: One address a day. You are the routing AI of a top-heavy sidewalk delivery robot in the fictional city of Costa Palma — the city wants you horizontal.

permissions:
  - reddit_api   # getCurrentUser() — attributes the daily-best record to a username
  - redis        # daily-best leaderboard (see src/main.tsx)

webroot: webroot
